# ── Database (same credentials as your craftalism postgres) ──────────────────
# These must match POSTGRES_USER and POSTGRES_PASSWORD in your main compose file
DB_USER=admin
DB_PASSWORD=123

# ── Auth Server ───────────────────────────────────────────────────────────────
# Public URL of this auth server (used as JWT issuer claim)
# In OCI: replace with your instance's public IP or domain
AUTH_ISSUER_URI=http://localhost:9000

# ── Minecraft Server Client ───────────────────────────────────────────────────
MINECRAFT_CLIENT_ID=minecraft-server
# Generate a strong secret: openssl rand -base64 32
MINECRAFT_CLIENT_SECRET=change_me_in_production

# ── RSA Keys ─────────────────────────────────────────────────────────────────
# Generate with: chmod +x generate-keys.sh && ./generate-keys.sh
# Paste the output lines below directly.
# The \n characters are literal — do not replace them with real newlines here.
RSA_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_BASE64_KEY_HERE\n-----END PRIVATE KEY-----
RSA_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nYOUR_BASE64_KEY_HERE\n-----END PUBLIC KEY-----
